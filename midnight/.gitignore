node_modules/
bun.lockb
*.log
.DS_Store

# Orchestrator runtime
.orchestrator-logs/
batcher-data/
midnight-level-db/

# Compiled contract assets that should be regenerated
# (Commented out — we DO want the managed/ output committed so the
# batcher can import it without running `compact compile` first.)
# packages/contracts-midnight/contract-anchor/src/managed/

# Secrets
.env
.env.*
!.env.example
